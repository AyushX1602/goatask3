# Pinned per rules.md engineering conventions — no open ranges.
# Every entry here has a prebuilt wheel for Windows / CPython 3.12 (verified 5 Sep 2026).

# Face core
onnxruntime
opencv-python
numpy
Pillow

# Perceptual hash for candidate dedupe
ImageHash

# Networking
requests
httpx
websockets

# CLI + console output (recording deliverable, see rules.md)
typer
rich

# Config + retries
python-dotenv
tenacity

# Evidence hashing (F7) — keccak256, EVM-compatible (differs from SHA3-256)
eth-hash[pycryptodome]

# Chain client (F8/F9) — talks to Anvil (required, D-27) or any EVM chain
web3

# Testing
pytest

# Local demo UI (architecture.md 5a)
fastapi
uvicorn[standard]
python-multipart
